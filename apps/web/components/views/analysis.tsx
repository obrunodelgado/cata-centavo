"use client";

/** Placeholder for ticket 09 — the dataset × chart-type explorer lands there. */
export function AnalysisView() {
  return (
    <section className="view enter" aria-label="Análises">
      <div className="card">
        <div className="card-title">Análises</div>
        <p className="card-sub">Explore seus dados por conjunto e tipo de gráfico — em construção.</p>
      </div>
    </section>
  );
}

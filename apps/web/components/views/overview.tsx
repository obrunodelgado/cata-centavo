"use client";

/** Placeholder for ticket 06 — the overview renders real aggregates there. */
export function OverviewView() {
  return (
    <section className="view enter" aria-label="Visão geral">
      <div className="card">
        <div className="card-title">Visão geral</div>
        <p className="card-sub">Acompanhamento da sua saúde financeira — em construção.</p>
      </div>
    </section>
  );
}

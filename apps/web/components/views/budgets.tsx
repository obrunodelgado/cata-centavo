"use client";

/** Placeholder for ticket 10 — the budgets grid lands there. */
export function BudgetsView() {
  return (
    <section className="view enter" aria-label="Orçamentos">
      <div className="card">
        <div className="card-title">Orçamentos</div>
        <p className="card-sub">Orçamentos por categoria — em construção.</p>
      </div>
    </section>
  );
}

"use client";

/** Placeholder for ticket 08 — bills, instalment plans and closing days land there. */
export function CardsView() {
  return (
    <section className="view enter" aria-label="Cartões">
      <div className="card">
        <div className="card-title">Cartões</div>
        <p className="card-sub">Faturas, parcelas e limites — em construção.</p>
      </div>
    </section>
  );
}

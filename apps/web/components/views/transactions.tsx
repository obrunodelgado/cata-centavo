"use client";

/** Placeholder for ticket 07 — the transaction list and CSV export land there. */
export function TransactionsView() {
  return (
    <section className="view enter" aria-label="Transações">
      <div className="card">
        <div className="card-title">Transações</div>
        <p className="card-sub">Suas movimentações com filtros e exportação CSV — em construção.</p>
      </div>
    </section>
  );
}
